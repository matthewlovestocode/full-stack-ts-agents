# Shared environment values consumed by tooling or both stacks.
# Replace placeholder values with project-specific configuration.
SHARED_API_BASE_URL=http://localhost:3000
FEATURE_FLAG_SAMPLE=disabled
