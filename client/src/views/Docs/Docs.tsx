export function Docs() {
  return (
    <section>
      <h1>Documentation Hub</h1>
      <p>All project guides live under the `/docs` directory alongside architecture, API, and workflow notes.</p>
      <p>
        Start with the <code>README.md</code> index to find the right guide, then drill into the frontend,
        backend, or database playbooks as needed.
      </p>
    </section>
  );
}

export default Docs;
