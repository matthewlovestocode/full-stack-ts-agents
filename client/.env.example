# Frontend-only variables must use the VITE_ prefix so Vite can inject them at build time.
# Copy to client/.env and customize for each environment.
VITE_API_BASE_URL=http://localhost:3000/api
VITE_FEATURE_FLAG_SAMPLE=false
